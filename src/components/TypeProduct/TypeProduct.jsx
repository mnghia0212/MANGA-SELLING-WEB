import React from 'react'

const TypeProduct = ({name}) => {
  return (
    <div style={{fontSize: '20px', fontFamily:'Pixelify Sans', padding:'0px 10px'}}>{name}</div>
  )
}

export default TypeProduct
